#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[]) {
  FILE *fp;
  if ((fp = fopen("nameGoesHere", "r")) == NULL) {
    printf("File error! It doesn't exist :(\n");

    exit(1);
  }

  fscanf(fp
  
  
  return 0;
}
