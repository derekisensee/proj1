#include <stdio.h>
#include <stdlib.h>
#define MAX_LENGTH 1000

int main(int argc, char* argv[]) {
  if (argc == 1) { // if no args passed
    printf("tgrep: searchterm [file...]\n");
  }

  // here: read from stdin (keyboard)
  if (argc == 2) {

  }

  if (argc == 3) {
    
  }
}
